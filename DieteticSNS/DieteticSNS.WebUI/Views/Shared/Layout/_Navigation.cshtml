@using DieteticSNS.Application.Common.Interfaces
@inject ICurrentUserService currentUserService

@{
    string dir = "~/img/uploads/";
    string noAvatar = "~/img/noavatar.jpg";
}

<div id="mainRow" class="row align-items-center bg-primary px-5">
    <div class="col-1"></div>
    <div class="col-auto py-3">
        <h3 id="mainHeading">
            @if (User.Identity.IsAuthenticated)
            {
                <a asp-controller="Posts" asp-action="GetPostList" class="text-white" style="text-decoration:none">
                    <b>Dieteticer</b>
                </a>
            }
            else
            {
                <a asp-controller="Home" asp-action="Index" class="text-white hoverable" style="text-decoration:none">
                    <b>Dieteticer</b>
                </a>
            }
        </h3>
    </div>
    <div class="col"></div>
    @if (User.Identity.IsAuthenticated)
    {
        <div class="col-auto">
            <h5 class="mt-1">
                <a asp-controller="Users" asp-action="GetProfile" asp-route-id="@currentUserService.GetUserId()" class="no-link hoverable">
                    <span id="userFullName">@currentUserService.GetUserFullName()</span>
                </a>
            </h5>
        </div>
    }
    <div class="col-auto">
        @if (User.IsInRole("Administrator"))
        {
            <span class="mx-3">
                <a asp-controller="Home" asp-action="AdminPanel" class="no-link hoverable">
                    <i class="fas fa-shield-alt fa-lg"></i>
                </a>
            </span>
        }

        <span class="mx-3">
            <a asp-controller="Posts" asp-action="GetMinifiedPostList" class="no-link hoverable">
                <i class="fas fa-search fa-lg"></i>
            </a>
        </span>

        <span class="mx-3">
            <a asp-controller="Users" asp-action="GetMinifiedUserList" class="no-link hoverable">
                <i class="fas fa-users fa-lg"></i>
            </a>
        </span>

        @if (User.Identity.IsAuthenticated)
        {

            @*////badge*@
            @*<span class="mx-3">
                    <a asp-controller="Home" asp-action="AdminPanel" class="no-link hoverable" style="position:relative">
                        <i class="fas fa-shield-alt fa-lg">
                            <span class="badge-custom">1</span>
                        </i>
                    </a>
                </span>

                <span class="mx-3">
                    <i class="fas fa-bell fa-lg" style="color: inherit; text-decoration:none; position:relative">
                        <span class="badge-custom">1</span>
                    </i>
                </span>*@

            @*<span class="badge badge-pill badge-danger">Danger</span>*@
            @*////badge*@



            <div class="dropdown d-inline mx-3">
                <a href="#" data-toggle="dropdown" class="no-link hoverable">
                    <i class="fas fa-cog fa-lg"></i>
                    <i class="fas fa-caret-down fa-lg"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right mt-4">
                    <a asp-controller="Account" asp-action="UpdateAccount" class="dropdown-item">Settings</a>
                    <a asp-controller="Account" asp-action="ChangePassword" class="dropdown-item">Change password</a>
                    <form id="deleteAccountForm" asp-controller="Account" asp-action="DeleteAccount" method="post" class="d-inline">
                        <button type="button" class="dropdown-item" data-toggle="modal" data-target="#deleteAccountModal">Delete account</button>
                    </form>
                    <div class="dropdown-divider"></div>
                    <form asp-controller="Account" asp-action="Logout" method="post">
                        <button type="submit" class="dropdown-item">Logout</button>
                    </form>
                </div>

                <div class="modal fade" id="deleteAccountModal" tabindex="-1">
                    <div class="modal-dialog modal-sm modal-dialog-centered">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title">Delete confirmation</h5>
                                <button type="button" class="close" style="outline: none" data-dismiss="modal"><span>&times;</span></button>
                            </div>
                            <div class="modal-body">
                                Are you sure to delete account?
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-danger" form="deleteAccountForm">Delete</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
        else
        {
            <span class="mx-3">
                <a asp-controller="Account" asp-action="Register" class="no-link hoverable">
                    <i class="fas fa-user-plus fa-lg"></i>
                </a>
            </span>

            <span class="ml-3">
                <a asp-controller="Account" asp-action="Login" class="no-link hoverable">
                    <i class="fas fa-sign-in-alt fa-lg"></i>
                </a>
            </span>
        }




        <div class="dropdown d-inline mx-3">
            <a href="#" data-toggle="dropdown" class="no-link hoverable">
                <i class="fas fa-bell fa-lg"></i>
            </a>
            <div class="dropdown-menu dropdown-menu-right mt-4" style="width: 400px">
                <h6 class="text-center">You have 0 notifications</h6>

                <div class="list-group list-group-flush list-group-scrollable">
                    <div class="list-group-item py-2 px-2">
                        <div class="d-flex w-100 justify-content-start">
                            @if (!String.IsNullOrEmpty(currentUserService.GetUserAvatarPath()))
                            {
                                <img class="rounded-circle img-fluid border border-dark" style="height: 3.5rem; width: 3.5rem" src="@currentUserService.GetUserAvatarPath()" asp-append-version="true" alt="Avatar">
                                @*<img class="rounded-circle img-fluid border border-dark" style="height: 3.5rem; width: 3.5rem" src="@String.Concat(dir, currentUserService.GetUserAvatarPath())" asp-append-version="true" alt="Avatar">*@
                            }
                            else
                            {
                                <img class="rounded-circle img-fluid border border-dark" style="height: 3.5rem; width: 3.5rem" src="@noAvatar" asp-append-version="true" alt="Avatar">
                            }
                            <div class="d-flex flex-column justify-content-between ml-2">
                                <div>
                                    <h6 class="mb-2 d-inline">
                                        <a asp-controller="Users" asp-action="GetProfile" asp-route-id="@currentUserService.GetUserId()" class="no-link">
                                            Kamil Dołęgiewicz
                                        </a>
                                    </h6>
                                    <span class="d-inline">is now following you.</span>
                                </div>
                                <small>February 2 at 7:54 PM</small>
                            </div>
                        </div>
                    </div>
                    <div class="list-group-item py-2 px-2">
                        <div class="d-flex w-100 justify-content-start">
                            @if (!String.IsNullOrEmpty(currentUserService.GetUserAvatarPath()))
                            {
                                <img class="rounded-circle img-fluid border border-dark" style="height: 3.5rem; width: 3.5rem" src="@currentUserService.GetUserAvatarPath()" asp-append-version="true" alt="Avatar">
                                @*<img class="rounded-circle img-fluid border border-dark" style="height: 3.5rem; width: 3.5rem" src="@String.Concat(dir, currentUserService.GetUserAvatarPath())" asp-append-version="true" alt="Avatar">*@
                            }
                            else
                            {
                                <img class="rounded-circle img-fluid border border-dark" style="height: 3.5rem; width: 3.5rem" src="@noAvatar" asp-append-version="true" alt="Avatar">
                            }
                            <div class="d-flex flex-column justify-content-between ml-2">
                                <div>
                                    <h6 class="mb-2 d-inline">
                                        <a asp-controller="Users" asp-action="GetProfile" asp-route-id="@currentUserService.GetUserId()" class="no-link">
                                            Kamil Dołęgiewicz
                                        </a>
                                    </h6>
                                    <span class="d-inline">is now following you.</span>
                                </div>
                                <small>February 2 at 7:54 PM</small>
                            </div>
                        </div>
                    </div>
                </div>

                <h6 class="text-center mt-2 mb-0">
                    <a asp-controller="Users" asp-action="GetProfile" asp-route-id="@currentUserService.GetUserId()" class="no-link">
                        View all
                    </a>
                </h6>
            </div>
        </div>


        @*<div id="suggestionCard" class="card my-4">
                <h5 class="card-header text-center">Suggested accounts</h5>
                <div id="suggestionList" class="list-group list-group-flush">
                    @foreach (var user in suggestedUsers.Users)
                    {
                        <div id="@("suggestionListItem_"+user.Id)" class="list-group-item py-3 pl-3 pr-2">
                            <div class="d-flex w-100 justify-content-start">
                                @if (!String.IsNullOrEmpty(user.AvatarPath))
                                {
                                    <img class="rounded-circle img-fluid border border-dark" style="height: 3.5rem; width: 3.5rem" src="@String.Concat(dir, user.AvatarPath)" asp-append-version="true" alt="Avatar">
                                }
                                else
                                {
                                    <img class="rounded-circle img-fluid border border-dark" style="height: 3.5rem; width: 3.5rem" src="@noAvatar" asp-append-version="true" alt="Avatar">
                                }
                                <div class="ml-3">
                                    <h6 class="mb-2">
                                        <a asp-controller="Users" asp-action="GetProfile" asp-route-id="@user.Id" class="no-link">
                                            @user.FirstName @user.LastName
                                        </a>
                                    </h6>

                                    <form asp-controller="Followings" asp-action="FollowUser" asp-route-userId="@user.Id" asp-route-followerId="@currentUserService.GetUserId()" method="post" data-ajax="true" data-ajax-method="post" data-ajax-success="setTimeout(deleteSuggestion, 300, @user.Id)">
                                        <button type="submit" class="btn btn-outline-dark btn btn-sm">Follow <i class="fas fa-user-plus"></i></button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>*@

    </div>
    <div class="col-1"></div>
</div>
